Frozen before GPU execution. Project baseline remains FlashTGN, specifically the immutable artifact and environment recorded in ../flash_tgn_rebaseline_20260909/input_manifest.json. The previous original and mailbox-repaired continuous replay gates remain FAILED. This small experiment does not relax them or claim full training parity.

Fixed scope: Wikipedia, original dimensions/optimizer/precision and auto schedule, K=50; batch_size 200/2000 × one/two layers = four cases. Capture two real training steps per case: zero-based step 8 after eight warm steps, and floor(num_train_batches/2)+1. Capture every attention layer at these positions, expecting 12 input sets. The source trains one complete epoch to supply real weights, memory, neighborhoods and loss gradients. Original mailbox behavior remains in this capture generator; fixed captured inputs, not separately racing trajectories, are the local reference. Do not interpret this as repairing source training semantics.

Capture literal input tensors to temporal_attention (q_proj, z_flat, hot_nbr, W_kv weight/bias), its original output, and its actual upstream loss gradient. Capture actual inputs and outputs to each fused_gather_encode_backward_v2 / fused_l0_gather_encode_backward_v2 invocation on the same selected steps. Freeze full tensors without subsampling rows or inventing gradients. Data/CPU serialization is outside all performance measurements and every input archive is hashed.

Source-noise test: repeat each captured CUDA backward call 16 times on byte-identical inputs. Preserve full output tuples, finite checks, byte comparisons and the unchanged 2e-4 + 2e-4*abs(reference) criterion. Report differences by output, particularly time-encoding parameter gradients. Recheck all inputs after replay for mutation. These tests isolate observed kernel nondeterminism, not all possible sources of training drift. No new tolerance based on observed noise.

Candidate: in temporal_attention's source DENSE branch only, replace W_kv(z_flat) with W_kv(z_flat[valid_mask]) then zero-initialize a dense projection buffer and scatter valid projected rows back. All source packed-branch operations, path choice, attention forward/backward kernels and mathematical ordering within those kernels stay unchanged. The literal AST must match source after replacing only that one assignment. No lower precision, new CUDA kernel, precomputed indices, cached predictions or omitted gradients. Dense invalid rows must be ignored by attention, and their input gradients must match the source. Pay masking, compaction, dynamic length synchronization, allocations, copy/scatter and all backward work.

Local qualification uses the same captured q/z/weights/actual upstream gradient in source A, source B and candidate, computing output and full gradients of q_proj, z_flat, W_kv weight and bias with torch.autograd.grad. Compare source A output to capture, source B and candidate to A; report full finite/shape/dtype/byte and original fixed tolerance results. Archive full results and independently recheck with CPU NumPy before timing. No full-trajectory qualification or quality claim follows from this local pass. If any local candidate case fails, report failure; any timing would be diagnostic only and no qualified combined gain may be reported.

Paid block timing only after independent CPU checks: nine randomized paired rounds, five complete forward+backward calls per interval; four warm calls per arm. Identical inputs resident in both arms, no profiling/events inside the block, full allocation and gradient production included. Use CUDA interval events plus synchronized wall time. Restoring inputs/model and loading archives occur outside timing. Report every input set, all-12 aggregate and the SOURCE-dense subset identified by frozen source policy, including unchanged packed cases. Target for further interest is >=1.10 local block ratio for the source-dense subset with no numeric failure, but this is not a whole-step target or admission to a full training speedup claim. Do not multiply local ratios by previous epoch shares.

Use GPU0 UUID GPU-9bd364a9-9d88-0b78-ef55-caac6f73d6b1 on pro6000-8, exclusive existing flash_rebaseline_gpu0.lock, idle/foreign-process guards and one CPU thread. Keep source FP32 matmul precision='high', CUBLAS workspace config and strict fused policy. Prior source, environment and sealed experiments stay read-only; all outputs in this new experiment. Preserve failures and fixed inputs. No extra candidate variants or precision search in this experiment.
