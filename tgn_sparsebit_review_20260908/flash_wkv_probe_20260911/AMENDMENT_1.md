Frozen after qualify_v1 reported all six changed dense cases failing, before executing a corrected candidate. The six source-packed identity cases pass. Source A/B and original captured forward are byte-identical in all 12 cases; this failure cannot be assigned to local source replay noise.

Source fused dense attention assigns invalid scores -1e9, then performs ordinary softmax. An entirely empty query therefore has uniform weights over its K padding positions, and its output/grad_V use their projected values. Source segmented attention explicitly returns zero for an empty segment, but that does not define source DENSE behavior. Consequently the initial assumption that all invalid positions are dead was false for empty queries. Preserve candidate.py, probe.py and qualify_v1 failure records. Independently check whether output failures lie in empty-query rows.

Authorize one targeted semantic correction to the same prototype: in the source-dense branch, project positions satisfying valid_neighbor OR query_has_no_valid_neighbors. Keep all K positions of an empty query, preserving source behavior, and scatter those projections back. Preserve the existing packed branch and the exact original attention kernels. Pay the extra mask/reduction and all previous costs. No attention semantic repair, tolerance change, precision change or additional variant search.

Use new candidate_keep_empty.py, probe_keep_empty.py and qualify_keep_empty_v1 output. Apply the identical fixed-input forward/full-local-gradient and independent CPU gate. Timing is permitted only if that corrected local gate passes; original full-training gates and first-candidate failures remain failed. This local test does not include downstream time-encoding gradients or whole training trajectories.

One attempted noise_v1 launch occurred before qualify_v1 finished serializing its archives; the exclusive GPU lock rejected it before GPU work. It is retained as a failed preflight. A prematurely started CPU audit also found qualification.json not yet written. After the qualification process completed, the identical noise inputs/code run as noise_v2 and CPU audit run again. No accuracy trial or input was changed by these orchestration corrections.
