Frozen after qualify_v1 and its independent CPU audit, before any follow-up GPU run.

The original baseline failed all 64 four-step replay comparisons under the unchanged tolerance. The sampler audit passed 3,584 sampled rows. First-step prediction/loss/memory/RNG are byte-identical between uninstrumented source arms in all eight cases; large violations arise after mailbox writes, with additional small backward discrepancies in two two-layer/K50 cases. Do not delete these results, loosen the tolerance, or call this baseline qualified.

Follow-up CPU oracle reconstructs each first-step mailbox from the archived common memory and immutable batch inputs. Source docstring specifies last-write-wins; interpret this as the final occurrence in the actual concatenated [src,dst] index order. Probe repeated original writes and explicit GPU integer-max winner selection on identical frozen inputs, checking both against sequential CPU assignment. Also probe writer-local PyTorch deterministic mode as a diagnostic, not a selected baseline replacement. Archive touched rows, complete input payloads and independent CPU comparisons.

Qualify FlashTGN + explicit mailbox winner selection in isolation using the same matrix, checkpoint sequence, raw capture and original numeric gate. This is a correctness repair and changes outcomes of undefined duplicate writes; do not label it equivalent to a particular original racing execution or an optimization. Original source remains read-only. Further residual fused backward nondeterminism is reported separately, without retries to obtain a passing sample.

If either replay fails, qualified speed comparisons remain disallowed. Nevertheless measure UNMODIFIED FlashTGN as a diagnostic baseline: three fresh-process, full-train-split, four-epoch campaigns and a separate two-epoch event profile over all eight configurations. These diagnose actual execution paths/costs despite known run-to-run trajectory differences. Report them explicitly as unqualified diagnostic timing, never as validated accuracy, quality equivalence or optimization speedup. Counts and full final state archives retained. This exception only opens diagnostic timing; the original fullrun.py gate remains intact and no claim of passing its contract is made.

Maintain exclusive GPU3 UUID/lock and idle/foreign-process checks. If another job is using it, wait while conducting independent CPU work. No original source, extension, data or previous result is modified.
