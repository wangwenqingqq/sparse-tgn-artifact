Frozen after all three diagnostic timing campaigns and the full event-profile campaign, before any kernel trace.

The event profile attributes 39.1–52.7% of epoch time to backward; much of this lies outside the named custom CUDA functions. Add one bounded diagnostic trace in each of the eight already declared cases to identify actual backward GPU operations and the forward modules owning their gradients. Run the unmodified source training entry for one full epoch with its original auto schedule policy. A torch.profiler schedule records one middle-epoch training step after one profiler warmup step. No shape is selected by a favorable timing result. Other steps supply a real trained state. This trace is not another performance measurement and does not establish numeric qualification.

Trace scopes add record_function annotations only. The active step synchronizes once after its mailbox write so that its complete GPU activities lie within the recorded step scope. Preserve complete Chrome traces and CPU UID/forward-sequence events; attribute CUDA activities using external IDs, runtime correlations, CPU containment and forward-backward flows. Report mapping coverage and ambiguities. Do not label unassigned backward work as GEMM or claim that existing matmuls are not already using Tensor Cores. Do not convert profiler kernel durations into an achieved epoch speedup.

Use the same guarded GPU0, frozen sources, environment and precision. Original source and all earlier GPU harness files remain unchanged; a separate trace runner and supervisor are added. Gate stays failed.
