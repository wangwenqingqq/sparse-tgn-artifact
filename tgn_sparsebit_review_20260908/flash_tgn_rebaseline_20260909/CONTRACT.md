**FlashTGN is the project baseline. This campaign re-establishes that baseline and measures its costs; no TNCN result is carried into a FlashTGN speed claim.**

Frozen before GPU execution, 2026-09-09. Source: the existing supplied Flash-TGN artifact under `/home/data/wangxuran/factor_tgn_sptc_20260902/vendor/flash-tgn-artifact`. Record content hashes of Python/CUDA sources and the actual extension binary. Git metadata is absent; do not infer a verified upstream revision from the historical commit label.

Use complete existing Wikipedia data: 157,474 chronological interactions, 9,227 nodes, original 172-dimensional edge features, zero 256-dimensional node features. Validate the existing source-data hashes and independently check TCSR endpoints, event IDs, timestamps and row chronology on CPU. No first-20k replacement, random feature fallback or smoke batch cap in performance runs.

Eight declared configurations: batch sizes 200 and 2000 × neighbors 20 and 50 × one and two layers. Keep embedding/time dimension 256, two heads, dropout 0.1, Adam lr 1e-4, seed 42, memory reset each epoch, and the artifact's normal sampling/negative-sampling/precision/attention policy. `precompute_mode=auto`, so the source may select different schedule policies for one and two layers. Preserve the source split function and report its batch-aligned boundaries for each case. FP32 plus source `set_float32_matmul_precision('high')` is the baseline contract; do not transfer the former TNCN TF32-off contract.

Factory construction is copied from the actual `train_flash_tgn.py` main function, stopping at the final `trainer.train()` to return its constructed Trainer. All preceding statements and source imports remain literal. Execute the actual `FlashTGNTrainer.train()` and `run_epoch()` methods for performance. An AST wrapper around the epoch body adds only outer timing and is mechanically checked by removing the wrapper and comparing the AST to source. No scheduling, dropout, forward, backward, optimizer, state update or mailbox arithmetic is replaced.

Before performance, establish source self-replay and transparent profiling replay on each configuration: eight warmup steps, restore one frozen state and four following continuous steps in each of three arms (source A, source B, event instrumentation). Preserve complete per-step raw parameters, gradients, Adam state, memory/mailbox, predictions, loss, embedding and RNG states. Report byte equality and a fixed FP32 test `abs(a-b) <= 2e-4 + 2e-4*abs(reference)`; discrete fields and RNG must match exactly, all floating fields must be finite. Do not widen tolerance after seeing results. An independent CPU/NumPy audit recomputes comparisons before qualifying any performance interpretation. Failed self-replay is a baseline-validity finding and must be investigated, not hidden by instrumented-run comparisons.

During qualification, additionally record up to 64 evenly spaced actual queries per temporal-sampler invocation. Independently reconstruct their returned neighbors/event IDs/times from source TCSR using strictly earlier timestamps, original order and padding. This is a bounded sampler audit, not an exhaustive kernel correctness proof. Record all query counts and sampled rows.

Trace actual CUDA extension calls, successes and failures, plus source dispatch counters in qualification and profile replays. Strict fused policy turns supported attention/gather failures into errors. LSE has its own source fallback; monitor its extension failures explicitly. Do not present existence of a fused function as evidence of its execution, or silently benchmark a failed fused path as the optimized baseline. Count-only monitors may remain in coarse timing and their small overhead must be stated.

Performance: three fresh-process campaigns, randomized case order within each; each case runs four complete training epochs. Keep epoch 1 as cold/warmup evidence and report all remaining nine epoch observations per case, median/p10/p90, rather than a speedup over any previous campaign. Include an outer epoch timer that covers state reset, schedule refresh/release, source empty-cache calls, online training and source per-epoch logging. Separately report source online-loop and schedule preparation times, complete train invocation wall time and setup costs. The artifact's printed training time alone excludes schedule refresh in precomputed modes.

Run separate two-epoch detailed event profiles over complete training splits. Distinguish inclusive and exclusive scopes; never sum nested timings. Report sampler/schedule, memory update/LSE/GRU, features, embedding/projections/attention, predictor/loss, backward, optimizer, mailbox and allocator boundaries. Profiling instrumentation adds cost: its phase fractions and hypothetical zero-cost projections are diagnostic opportunity estimates, not realized speedups or mathematical hardware bounds. Do not divide instrumented component times by a different uninstrumented denominator.

No new kernel or paid optimization is preselected. If the previous cache proposal is structurally absent or its actual FlashTGN boundary lacks room, report that result and identify the measured next target. Do not claim full training-quality parity, original-TGN equivalence, Tensor Core benefit, or a >=1.10 speedup from baseline timing alone. Real scikit-learn is installed in this isolated experiment; performance is train-only and AP/AUC zeros there are not evaluation results.

Use the established exclusive GPU3 lock and UUID `GPU-a149f5af-55ab-ce33-8d3d-371a7ae61dd2` on pro6000-8, an NVIDIA RTX PRO 6000 Blackwell Server Edition. Preflight idle and process checks, one CPU thread and CUBLAS workspace configuration. Other GPUs and shared services remain untouched. Preserve failed attempts, all raw logs, baseline/data hashes, transferred metadata and remote tensor archives. Original FlashTGN, TNCN and all preceding experiments remain read only.
