All GPU attempts are retained under evidence/ and in complete remote output/ archives. A successful process exit is not a passing accuracy result.

- qualify_v1: original FlashTGN self replay and instrumented replay. Process completed; all 64 comparisons failed. Independent CPU audit confirmed all failure lists; sampled temporal index audit passed.
- mailbox_cpu first invocation: CPU computation printed all eight cases, then failed to write final JSON because the newly created remote experiment did not yet have an analysis/ directory. No GPU was initialized. Created that experiment directory and reran the identical script; no numerical logic or input changed. Successful full stdout and JSON retained; initial FileNotFoundError recorded here.
- mailbox_probe_v1: eight cases × three writer variants × 16 identical-input trials. Original 0/128 matched the serial CPU oracle; writer-local deterministic mode and explicit last-position selection each 128/128. No trials discarded. Independent CPU archive audit matched all logged outcomes.
- qualify_repaired_v1: explicit last-position selection only. Process completed; fixed original tolerance retained. 11/64 comparisons passed, none byte-identical overall, and all multi-step case pairs failed as a whole. First-step forward/state equality was restored; backward numeric drift remained and grew in subsequent steps. Independent CPU audit confirmed outcomes. No passing seed or relaxed tolerance was selected.
- diagnostic_timing_v1/v2/v3 and diagnostic_profiles_v1: execute unmodified FlashTGN under the explicit diagnostic exception in AMENDMENT_1.md. These do not pass or replace the original qualification gate. Each process result, configuration, actual extension counts, final raw state, and GPU before/after snapshots are retained.

GPU3 was used for the original qualification; unrelated tasks subsequently took it. Follow-up execution moved to idle GPU0 under AMENDMENT_2.md. No unrelated process was stopped and no original source, environment, data or earlier experiment was edited.

- kernel_trace_v1: eight full source training epochs with one middle step recorded per case, all first-run process completions successful. The profiler warns that it clears events at cycle end; this campaign has one active cycle and serializes its events inside on_trace_ready, so the retained active-cycle records are complete.
- Postprocessing only: the initial analyze.py duplicated the rep keyword in already-tagged completion records; changed dictionary construction before producing final summaries. No GPU data changed or reran.
- Postprocessing only: analyze_traces_v1.py initially treated inner CUDA annotations in Chrome as stronger than an enclosing autograd engine, unlike its CPU UID ownership rule (20/40 crosscheck differences per trace). It also lacked an External-id-free direct-launch fallback (one LSE launch per trace). Final analysis aligns both trees' autograd priority and uses exact runtime correlation plus unique CPU containment for those eight launches. All 1,556 activities now link, with zero remaining ambiguity/crosscheck mismatch. The initial analyzer and summary are retained, and the original traces are unchanged.
