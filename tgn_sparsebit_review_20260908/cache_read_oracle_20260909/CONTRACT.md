Measure the idealized whole-step opportunity from eliminating message-cache lookup and concatenation on the qualified batched-store direct-CSR training path. This is a diagnostic oracle, not a deployable optimization or a mathematical hard bound.

Use the same 12 frozen checkpoints, eight consecutive steps per window, original source-compatible FP32 arithmetic/autograd organization, determinism settings, and an idle GPU3 selected by UUID under the existing lock and process guard. Keep prior projects and evidence read only. No no-grad state-update variant is adopted.

Capture, from actual paid _compute_msg calls, only n_id, direction and the four concatenated constant fields src/dst/t/raw_msg, immediately after the literal source prefix. Assert every captured field has requires_grad=False. There are exactly four calls per step, source/destination for memory read and state advance. No learned time encoding, messages containing memory, GRU outputs, embeddings, relations, predictions, losses or gradients may be cached.

The free_cache_reads oracle replaces only that prefix (n_id host conversion, dictionary lookup, unpack, four cat operations) with resident captured tensors. Copy the remaining source statements literally: compute time differences and encoding, gather current memory, and form messages. Preserve aggregation, GRU, state writeback, the existing batched message-cache writes, neighbor updates, backward and Adam. Verify per-call n_id and direction in qualification; count exact calls in every timed replay. Maintain both logical message stores through normal paid writes.

Qualification: capture replay, plain paid baseline, and free_cache_reads each compare all 119 fields against the previous direct archive and the original 107 fields against source, at every one of 96 steps, using actual dtype and bytes. Save complete checks and captured records. Independently on CPU with CUDA hidden, reconstruct every captured prefix from the checkpoint's logical stores (first step) or preceding archived logical stores (later steps), selecting n_id in order and concatenating per-node data. Reaudit every training result and tensor-file hash before timing. Do not loosen the gate after failures.

Timing: paid batch_store versus free_cache_reads, nine randomized paired rounds/window, eight full steps/interval, 216 intervals and 1728 steps. Restore, warmup, GC and loading/capturing the oracle occur outside timing. Keep the same oracle record tensors resident in both arms for comparable retained allocations. Oracle cursor bookkeeping, real training arithmetic, normal cache writes and all other costs are paid. Report that precomputed buffers and altered lifetimes/cache behavior make this an idealized opportunity measurement, not a strict upper-bound proof.

Separately instrument eight-step replays to verify the intended prefix is removed, the remaining stages still execute, and all 115 persistent fields match at each window end. Report prefix record count, data volume and GPU-resident storage cost; these are oracle overhead, not a proposed production cache size.

Decision criterion, fixed before GPU work: use the geometric mean across the 12 window paired-ratio medians. If it is below 1.10, do not develop a complex cache-read kernel/layout to chase an overall 1.10x result on this workload. Retain all per-window results, including any individual case above 1.10; do not select only successful shapes. A point estimate above 1.10 merely admits further work and still needs uncertainty and implementation cost considered. Bootstrap intervals are descriptive within this one GPU session.

No full-epoch, long-run batch-store retention, downstream-quality, original-TGN entry-point, or Tensor Core claim. In user-facing labels write batch_size=32/200, not B32/B200 as possible GPU model names.
